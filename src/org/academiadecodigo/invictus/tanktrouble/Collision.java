package org.academiadecodigo.invictus.tanktrouble;

public class Collision {

    // Only after walls position it's possible to build method
    /* in car crash is something like that

    private Car[] cars;

    public CollisionDetector(Car[] cars) {
        this.cars = cars;
    }

    public boolean isUnSafe(GridPosition pos) {

        for (Car c : cars) {

            if (c.getPos()!= pos && c.getPos().equals(pos)) {
                return true;
            }

        }

        return false;
     */


}
